# react-router-dom's bundled dev sourcemaps reference a source file (index.mjs)
# that isn't shipped in the published package, which source-map-loader treats
# as a hard build error under CRA. Disabling sourcemaps sidesteps it.
GENERATE_SOURCEMAP=false
